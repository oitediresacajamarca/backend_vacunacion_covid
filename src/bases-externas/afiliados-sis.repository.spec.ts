import { AfiliadosSisRepository } from './afiliados-sis.repository';

describe('AfiliadosSisRepository', () => {
  it('should be defined', () => {
    expect(new AfiliadosSisRepository()).toBeDefined();
  });
});
