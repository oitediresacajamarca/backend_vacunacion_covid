import { VacunadosCovidFastEntity } from './vacunados-covid-fast.entity';

describe('VacunadosCovidFastEntity', () => {
  it('should be defined', () => {
    expect(new VacunadosCovidFastEntity()).toBeDefined();
  });
});
