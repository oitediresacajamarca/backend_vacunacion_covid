import { VacunadosCovidRepository } from './vacunados-covid.repository';

describe('VacunadosCovidRepository', () => {
  it('should be defined', () => {
    expect(new VacunadosCovidRepository()).toBeDefined();
  });
});
