import { StockIpressRepository } from './stock-ipress.repository';

describe('StockIpressRepository', () => {
  it('should be defined', () => {
    expect(new StockIpressRepository()).toBeDefined();
  });
});
