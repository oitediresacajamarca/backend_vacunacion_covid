import { EnviosRedRepository } from './envios-red.repository';

describe('EnviosRedRepository', () => {
  it('should be defined', () => {
    expect(new EnviosRedRepository()).toBeDefined();
  });
});
