import { CuadroAlmacenRedEntity } from './cuadro-almacen-red.entity';

describe('CuadroAlmacenRedEntity', () => {
  it('should be defined', () => {
    expect(new CuadroAlmacenRedEntity()).toBeDefined();
  });
});
