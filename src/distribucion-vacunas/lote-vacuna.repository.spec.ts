import { LoteVacunaRepository } from './lote-vacuna.repository';

describe('LoteVacunaRepository', () => {
  it('should be defined', () => {
    expect(new LoteVacunaRepository()).toBeDefined();
  });
});
