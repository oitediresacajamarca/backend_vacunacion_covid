import { DistribucionRedEntity } from './distribucion-red.entity';

describe('DistribucionRedEntity', () => {
  it('should be defined', () => {
    expect(new DistribucionRedEntity()).toBeDefined();
  });
});
