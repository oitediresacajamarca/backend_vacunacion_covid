import { RecepcionAnexosRepository } from './recepcion-anexos.repository';

describe('RecepcionAnexosRepository', () => {
  it('should be defined', () => {
    expect(new RecepcionAnexosRepository()).toBeDefined();
  });
});
