import { RecepcionAnexosEntity } from './recepcion-anexos.entity';

describe('RecepcionAnexosEntity', () => {
  it('should be defined', () => {
    expect(new RecepcionAnexosEntity()).toBeDefined();
  });
});
