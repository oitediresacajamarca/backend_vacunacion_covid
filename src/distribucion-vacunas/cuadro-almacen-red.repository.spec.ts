import { CuadroAlmacenRedRepository } from './cuadro-almacen-red.repository';

describe('CuadroAlmacenRedRepository', () => {
  it('should be defined', () => {
    expect(new CuadroAlmacenRedRepository()).toBeDefined();
  });
});
