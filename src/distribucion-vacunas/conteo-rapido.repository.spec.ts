import { ConteoRapidoRepository } from './conteo-rapido.repository';

describe('ConteoRapidoRepository', () => {
  it('should be defined', () => {
    expect(new ConteoRapidoRepository()).toBeDefined();
  });
});
